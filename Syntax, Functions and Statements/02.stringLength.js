function strLength(f, s, t) {
    let sum = f.length + s.length + t.length;
    let avr = Math.floor(sum / 3);
    console.log(sum);
    console.log(avr);
}

strLength('chocolate', 'ice cream', 'cake');